# Don't check auto-generated stuff into git
coverage
build
node_modules
stats.json

# Cruft
.DS_Store
npm-debug.log
.idea

typings/
.chrome

# Never commit a file that could be the admin sdk cert
*adminsdk*.json
